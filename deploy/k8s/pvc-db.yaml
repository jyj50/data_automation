apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rag-db-pvc
  namespace: rag-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
