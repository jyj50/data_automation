# NOTE: selector updated from rag-django → rag-backend to match deployment label
apiVersion: v1
kind: Service
metadata:
  name: rag-backend          # TODO: update Ingress backend.service.name to rag-backend
  namespace: rag-app
spec:
  selector:
    app: rag-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
