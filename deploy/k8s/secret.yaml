apiVersion: v1
kind: Secret
metadata:
  name: rag-secret
  namespace: rag-app
type: Opaque
stringData:
  SECRET_KEY: "please-change-me"   # TODO: generate with `python -c "import secrets; print(secrets.token_hex(32))"`
  LLM_API_KEY: ""                  # TODO: set if your LLM endpoint requires an API key
